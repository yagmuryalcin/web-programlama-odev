<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - wep_programlama_odev</title>

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/wep_programlama_odev.styles.css" asp-append-version="true" />
</head>

<body>
    <div class="app-shell">

        <!-- SIDEBAR -->
        <aside class="sidebar">
            <div class="sidebar-brand">
                <div class="brand-title">PROJE YÖNETİM</div>
                <div class="brand-subtitle">wep_programlama_odev</div>
            </div>

            <nav class="sidebar-nav">
                <a class="sidebar-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Home" ? "active" : "")"
                   asp-controller="Home" asp-action="Index">
                    <span class="sidebar-icon">🏠</span>
                    <span>Anasayfa</span>
                </a>

                @* Projeler/Görevler: login olan herkes görebilir, CRUD admin *@
                <a class="sidebar-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Projects" ? "active" : "")"
                   asp-controller="Projects" asp-action="Index">
                    <span class="sidebar-icon">📁</span>
                    <span>Projeler</span>
                </a>

                @* Görevler sayfası sadece Admin ise menüde görünsün (sende controller Admin) *@
                @if (User.IsInRole("Admin"))
                {
                    <a class="sidebar-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "TaskItems" ? "active" : "")"
                       asp-controller="TaskItems" asp-action="Index">
                        <span class="sidebar-icon">✅</span>
                        <span>Görevler</span>
                    </a>
                }

                <div class="sidebar-sep"></div>

                <a class="sidebar-link @(ViewContext.RouteData.Values["action"]?.ToString() == "Privacy" ? "active" : "")"
                   asp-controller="Home" asp-action="Privacy">
                    <span class="sidebar-icon">🔒</span>
                    <span>Gizlilik</span>
                </a>
            </nav>

            <div class="sidebar-footer">
                <div class="sidebar-footer-card">
                    <div class="footer-title">Hesap</div>
                    <div class="footer-subtitle">
                        @if (User?.Identity?.IsAuthenticated == true)
                        {
                            <span>@User.Identity.Name</span>
                        }
                        else
                        {
                            <span>Giriş yapılmadı</span>
                        }
                    </div>
                </div>
            </div>
        </aside>

        <!-- MAIN -->
        <div class="main">
            <!-- TOPBAR -->
            <header class="topbar">
                <div class="topbar-left">
                    <span class="topbar-page">@ViewData["Title"]</span>
                </div>

                <div class="topbar-right">
                    <partial name="_LoginPartial" />
                </div>
            </header>

            <!-- CONTENT -->
            <main class="content">
                @RenderBody()
            </main>

            <!-- FOOTER -->
            <footer class="app-footer">
                <div class="text-muted">
                    © @DateTime.Now.Year - wep_programlama_odev
                </div>
            </footer>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
